[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "auto-incucyte"
version = "0.2.0"
description = "Turn raw Incucyte exports into normalized tables and plots"
readme = "README.md"
requires-python = ">=3.10"
authors = [{name = "J. M. Warrington"}]
license = {file = "LICENSE"}
classifiers = [
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
]
dependencies = [
  "matplotlib>=3.7",
  "numpy>=1.24",
  "pandas>=2.0",
]

[project.scripts]
auto-incucyte = "auto_incucyte.analysis:main"

[tool.setuptools.packages.find]
where = ["src"]
